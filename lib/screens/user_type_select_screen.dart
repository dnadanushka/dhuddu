import 'package:flutter/material.dart';

class UserTypeSelectScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('User Type Select Screen'),
      ),
    );
  }
}
