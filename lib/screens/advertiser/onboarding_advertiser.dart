import 'package:flutter/material.dart';

class OnBoardingAdvertiserScreen extends StatelessWidget {
  static const routeName = '/on-bording-advertiser';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Advertiser'),
      ),
    );
  }
}
